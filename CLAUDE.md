# 다이브 로그 앱 — 프로젝트 브리프

이 문서는 이어서 작업할 때 읽는 기준 문서다. 여기 적힌 결정은 이미 논의를 거친 것이고,
바꾸려면 이유가 있어야 한다. 새 결정을 하면 이 문서에 추가한다.

---

## 1. 무엇을 만드는가

다이버가 **다이빙이 끝난 뒤 몰아서** 로그를 쓰고, 그 결과가 인스타에 올릴 만한
세로 카드로 나오는 앱.

**만들지 않는 것:** 다이빙 포인트 정보 앱. 수심·조류·시야 데이터를 우리가 제공하면
사람이 다칠 수 있다. 모든 데이터는 사용자가 자기 경험으로 입력한 것이고, 우리는
그것을 보여주기만 한다. 포인트 정보는 로그가 충분히 쌓인 뒤에나 생각할 일이다.

---

## 2. 절대 어기지 말 것

이 앱이 실패하는 방식은 정해져 있다. 아래 네 가지가 방어선이다.

**① 필수 입력은 세 개뿐이다 — 날짜, 장소, 종목(프리/스쿠버).**
나머지는 전부 선택이다. 수심, 시간, 수온, 시야, 공기압, 생물. 실제 사용자는 여행이
끝난 저녁에 몰아서 쓰기 때문에 대부분 기억나지 않는다. 필수 항목을 늘리는 순간
이탈한다. 기존 로그북 앱들이 전부 여기서 죽었다.

**② 빈 값이 있어도 결과물이 멀쩡해야 한다.**
안 채운 칸은 "비어 보이는" 게 아니라 **아예 사라져야** 한다. 카드 하단 정보 칸이
4개였다가 3개가 되고, 남은 칸이 자리를 나눠 갖는다. 무엇을 안 적었는지 카드에
티가 나면 안 된다. 플레이스홀더, 회색 대시, "정보 없음" 전부 금지.

**③ 입력 단위는 다이브가 아니라 트립이다.**
"제주 3일 8다이브"를 저녁에 한 번에 쓴다. 트립을 만들고 그 안에 다이브를 툭툭
추가하는 구조. 다이브 하나 쓰고 저장하고 나가고 다시 들어오는 흐름이면 안 된다.

> ~~트립 전체 요약 카드도 별도로 나온다 — 실제로 이게 개별 다이브 카드보다 더 잘 올라간다.~~
> **이 문장은 틀렸다.** 실제로 만들어 보고 뺐다 — §12 참조.
> 여기서 유효한 건 **입력 단위**가 트립이라는 것뿐이고, 출력(카드) 단위는 다이브다.

**④ "의미 있는 생물"은 앱이 판단하지 않는다.**
흔한 종 / 귀한 종 목록을 만들지 마라. 300번째 다이브의 흔한 놀래기가 첫 다이브인
사람에겐 인생 장면이다. 생물 입력 옆의 하트를 사용자가 직접 누르고, 누른 것이
그날의 하이라이트가 된다.

---

## 3. 카드 주인공 결정 로직

카드는 큰 것 하나를 보여줄 때 제일 예쁘다. 그 자리에 뭐가 들어갈지는 자동으로 정한다.

```
if (하이라이트로 지정한 생물이 있음)  → 생물형   (생물명이 주인공)
else if (종목 === '프리다이빙')        → 수심형   (최대 수심이 주인공)
else if (종목 === '스쿠버')            → 횟수형   (누적 다이브 수가 주인공)
```

사용자가 카드에서 탭해서 바꿀 수 있게 한다. 대부분 안 바꾸고, 바꾸는 사람은 이유가 있다.
**구현됨** — 히어로 글자를 탭하면 카드 아래에 후보가 열린다 (§10-㉜).

근거: 프리다이빙은 수심이 기록이고, 스쿠버는 누적 횟수가 자산이다. 생물은 매번
보는 게 아니라서, 본 날엔 그게 둘 다 밀어내는 게 실제 감각과 맞는다.

**생물 입력 UX:** 전체 어종 도감 검색은 무겁고 자유 입력은 오타가 난다.
① 내가 전에 적은 것 ② 같은 지역에서 많이 적힌 것 을 위에 띄워서 두 번 탭으로 끝낸다.
그래도 없으면 자유 입력을 허용한다.

---

## 4. 데이터 모델 (초안)

```ts
type Trip = {
  id: string
  title: string          // "제주 3박" — 비면 지역명+날짜로 자동 생성
  region: string         // "서귀포 · 제주"
  startDate: string
  endDate: string
  dives: Dive[]
}

type Dive = {
  id: string
  date: string           // 필수
  time?: string
  site: string           // 필수 — "문섬 새끼섬"
  discipline: 'free' | 'scuba'   // 필수

  maxDepth?: number      // m
  duration?: number      // 분 (프리는 초 단위일 수 있음 — 종목별 표기 분기 필요)
  waterTemp?: number     // °C
  visibility?: number    // m
  airEnd?: number        // bar, 스쿠버만
  style?: string         // 'CWT' | 'FIM' | ... 프리만

  sightings: Sighting[]
  photoUri?: string      // 메인 1장
  note?: string
  diveNumber?: number    // 스쿠버 누적 — 사용자가 시작 번호를 한 번 입력하면 자동 증가
}

type Sighting = {
  name: string           // "가시해마"
  scientificName?: string
  count?: number
  isHighlight: boolean   // 하트
  memo?: string          // "처음 봄"
}
```

**주의:** `diveNumber`. 이미 300번 넘게 탄 사람이 앱을 처음 켠다. 온보딩에서
"지금까지 몇 번 하셨어요?" 한 번 묻고 거기서부터 센다. 1번부터 시작하게 만들면
경력자가 바로 나간다.

---

## 5. 디자인 토큰

`dive-log-cards.html`이 시각 기준이다. 코드를 그대로 쓰지 말고 참조만 해라 —
그건 시안이지 컴포넌트가 아니다. 치수는 전부 640px 카드 높이를 전제한 매직 넘버이고
**일부는 서로 맞지도 않는다** (§10-⑥). 가져오는 건 색·비율·타이포 위계·눈금자 형태뿐이다.

```css
--abyss:  #050E1C   /* 카드 바탕 */
--deep:   #0B2A47
--mid:    #1B4C7A
--shal:   #3E86C4
--foam:   #C9E2F5
--paper:  #EAF1FA   /* 카드 본문 텍스트 */
--point:  #FF5C7A   /* 액센트(코럴) — 카드당 한 곳에만 */
--danger: #C93B2B   /* 지우기·경고 전용. 액센트가 아니다 */
--line:   rgba(201,226,245,.18)
```

- 숫자·라벨: **IBM Plex Mono** (다이브 컴퓨터 계기판 느낌)
- 한글: **Pretendard**
- 카드 비율: **9:16** 고정 (스토리 그대로)

**앱 화면은 밝고, 카드는 어둡다.** 위 팔레트는 **카드의 것**이다.
초기에는 화면도 같은 심해색(`--abyss`)을 썼는데 칙칙했다 — 배경과 면의 차이가 거의 없고
글자가 전부 흐린 회색이라 정보가 안 읽혔다. 화면은 밝은 물빛(`#F1F6F6` 배경 / 흰 면 /
`--deep` 글자)으로 뒤집었다. 부수 효과가 더 좋다: **카드가 배경에서 확 떠 보인다.**
결과물이 주인공인 앱이니 이 대비가 맞다.
- 화면 글자는 `inkA()`(= `--deep`), 카드 안은 그대로 `paper`/`foam` (`src/ui/theme.ts` 참조)
- **화면 액센트와 카드 액센트는 다른 색이다.** 화면은 네이비 계열 `#1D5FB0`
  (`surface.accent`), 카드는 코럴 `--point`. 화면은 브랜드 색으로 차분하게 가고
  **코럴은 결과물에만** 쓴다 — 카드가 배경에서 더 떠 보인다.
- 한 곳 규칙은 **카드에만** 적용된다. 화면에서는 하트와 "카드 보기" 버튼이 쓴다.

**액센트(`--point`)는 카드당 한 군데만.** 여기저기 쓰면 시선이 흩어진다.
`accentTarget()`이 수심 마크와 kicker 중 한 곳에만 배정한다.

> **주황(#FF7A2F) → 파랑(#4DA3FF) → 코럴(#FF5C7A)로 두 번 옮겼다.**
> 파랑은 실패였다. 파란 물 위의 파란 점이 안 보여서 물기둥 채도를 슬레이트까지 뺐는데,
> 물이 회색이 되고도 점은 여전히 안 튀어 **양쪽을 다 잃었다.**
> 코럴로 옮기면서 물기둥을 네이비로 되돌렸다. 붉은 계열은 물속에서 주황보다 **먼저**
> 사라지므로 "수면 밖 기록"이라는 시안의 원래 근거가 오히려 더 정확해진다.
>
> ⚠️ **액센트와 물색은 한 몸이다.** 한쪽만 옮기면 배경과 같은 계열의 점이 되어
> 눈금자(이 앱의 서명)가 무력화된다. 파랑 시절이 정확히 그랬다.

**수심 눈금자가 이 앱의 서명이다.** 카드 왼쪽 세로축, 0~40m 눈금, 액센트 점이 최대 수심.
숫자를 못 읽는 크기에서도 깊이가 읽힌다. 다른 로그 앱에 없는 유일한 요소니까
함부로 빼거나 축소하지 마라.

**사진이 없을 때:** 대체 이미지나 아이콘을 넣지 마라. 수심에 따라 아래쪽이 어두워지는
물기둥 그라데이션이 배경이 되고, 눈금자와 숫자가 주인공이 된다. 깊이 들어간 날일수록
카드가 어둡다 — 정보가 그래픽이 되는 구조다.

물기둥은 한 번 전체적으로 밝혔다(너무 탁했다). 대신 **글자가 앉는 위·아래만 누르는
약한 scrim**을 넣어야 했다 — 원래는 물기둥이 충분히 어두워서 scrim이 없었는데,
밝히자마자 흰 글자가 안 읽혔다. 가운데 물빛은 살리고 양끝만 누른다
(`columnScrimStops`). 깊이에 따른 명암 차이는 그대로 유지된다 — 18m 카드와 38m 카드를
나란히 놓으면 눈에 띄게 다르다.

**물기둥은 네이비다.** 한동안 슬레이트(회청)였는데, 그건 액센트가 파랑이던 시절의
땜질이었다. 코럴 액센트로 옮기면서 채도를 시안 수준으로 되돌렸다 — 코럴은 네이비의
보색이라 썸네일 크기에서도 점이 살아남는다.

---

## 6. 스택

- **Expo (React Native)** — 사진 접근과 이미지 저장이 핵심 기능이라 웹보다 유리
- 저장: 로컬 우선(SQLite/AsyncStorage). 계정·서버는 나중에.
- 카드 이미지 저장: `react-native-view-shot` 으로 뷰를 PNG로 캡처
- 지도는 지금 단계에서 **넣지 않는다** (아래 8번 참조)

웹으로 갈 거면 Next.js + `next/og` 도 가능하지만, 카메라롤 접근이 어색해진다.

---

## 7. 1주차 범위

이것만 되면 첫 검증이 가능하다. 그 외 전부 나중.

1. ~~트립 생성 → 다이브 여러 개 추가 → 저장~~ ✅
2. ~~필수 3개만으로 저장 가능, 나머지 비워도 정상 동작~~ ✅
3. ~~사진 1장 첨부~~ ✅ — 피커 UI까지 시뮬레이터에서 **끝까지 눌러 확인**했다
   (§10-㊸로 자동화가 뚫렸다). 9:16 크롭은 피커가 못 해서 직접 자른다 (§10-㊹).
4. ~~카드 3종 렌더링 + 주인공 자동 결정~~ ✅
5. ~~카드를 이미지로 저장~~ ✅ — 시뮬레이터에서 **1080×1920 실측**, 사진첩에 실제로
   들어가는 것까지 확인 (`saved=true`, DCIM에 1080×1920 PNG 생성).

1·2·4는 브라우저에서 실제로 눌러 가며 확인했다 (온보딩 → 트립 → 다이브 2개 → 카드,
누적 번호까지). 3·5는 네이티브 전용 모듈이라 시뮬레이터/실기기가 있어야 확인된다.

**검증 방법:** 만든 사람이 다음 다이빙 가서 직접 써본다. 그리고 다이빙 아는 지인
10명에게 보여주고 **실제로 저장해서 올리는지** 본다. "좋다"는 말은 신호가 아니다.
저장 버튼을 누르는지가 신호다.

---

## 8. 나중 (지금 하면 망하는 것)

- **지도** — 로그가 쌓이기 전엔 빈 지도다. 첫 사용자가 핀 0개를 보면 다시 안 온다.
  로그가 모이면 그때 자연스럽게 지도가 생긴다. 순서를 뒤집지 마라.
- **계절 탭 (물놀이/눈)** — 빈 화면이 하나에서 여러 개로 늘어날 뿐이다.
- 소셜 기능, 팔로우, 피드
- 다이브 컴퓨터 연동
- 계정/로그인 — 로컬 저장으로 충분히 오래 버틸 수 있다

---

## 9. 미해결

- **생물 제안 ②("같은 지역에서 많이 적힌 것").** 지금은 ①(내가 전에 적은 것)만 된다.
  다른 사용자 데이터가 없어서 만들 수가 없다. 서버가 생기기 전까지는 열리지 않는 항목.
- **예시 생물 데이터.** 시안의 남방큰돌고래는 실제로 다이빙 중 만나기 어려운 종이라
  자리만 채운 것이다. 실제 포인트에서 뭘 보는지에 맞춰 교체 필요.
  (`src/model/fixtures.ts`에 같은 주석을 달아 뒀다.)
- ~~이미지 피커 UI만 미검증~~ **해소됐다** — §10-㊸(idb) 참조.
- **Android 미검증.** 지금까지 확인은 전부 웹과 iOS 시뮬레이터다. 환경이 아예 없다
  (SDK·에뮬레이터·`android/` 없음, JDK가 8이라 17로 올려야 함). 다운로드만 10~15GB라
  미뤄 뒀다 — §7의 검증은 "다음 다이빙 가서 직접 써보기"이고 그건 iOS로 충분하다.
  **코드 감사는 해 뒀고 알려진 Android 함정(§10-⑩⑪⑫㉖, `collapsable`)은 전부 막혀 있다.**
  실제로 눌러 봐야 아는 건 둘이다:
  - `DateField`의 Android 분기 — `DateTimePickerAndroid.open()`은 iOS와 **완전히 다른 코드 경로**다
  - 폰트 패밀리가 Android에서 실제로 잡히는지 (§10-⑩의 원래 우려 지점)

---

## 10. 구현하며 정한 것

시안을 RN으로 옮기며 스펙에 없던 판단이 필요했던 지점들. 되돌리려면 이유가 있어야 한다.

**① 카드는 `width` 하나만 받고 나머지 치수를 전부 유도한다.**
`makeScale(width)`가 시안의 360×640 좌표계를 실제 치수로 바꾼다. 화면에서는 화면 폭으로,
저장할 때는 1080으로 **같은 컴포넌트를 렌더**한다 — 뷰를 확대하는 게 아니라 1080 해상도로
진짜 렌더하는 것이라 9px 라벨도 뭉개지지 않는다. 이 구조가 무너지면 저장 품질이 무너진다.

**② 액센트는 히어로별로 한 자리에 배정한다.** (시안과 스펙이 충돌해서 스펙을 따랐다)
시안은 kicker와 눈금 마크 두 곳에 쓰지만 §5는 "카드당 한 군데만"이다.

| 히어로 | 액센트 자리 |
|---|---|
| 수심형 / 횟수형 | 눈금 마크 (서명 요소) |
| 생물형 | kicker |
| 수심을 안 적은 카드 | kicker |

`accentTarget()`이 이걸 보장한다. 카드당 정확히 하나.

**③ 주인공이 비는 경우를 메웠다 — `site` → `date` 히어로.**
§3 로직은 "프리 → 수심, 스쿠버 → 횟수"인데, 프리인데 수심을 안 적었거나 스쿠버인데
시작 번호를 아직 안 넣으면 가리킬 값이 없다. 그대로 두면 "MAX DEPTH" 밑이 비는데
이건 §2-② 위반이다. 서로 넘겨주고, 둘 다 없으면 **장소명이 주인공**이 된다.

> ~~(장소는 필수)~~ **이 전제가 틀렸다.** 장소는 §2-①의 필수 3개지만 `addDive`가
> `site: ''`로 만든다 — **다이브를 추가만 하고 아무것도 안 적은 상태**가 정확히 이것이고,
> 그때 카드에 "SCUBA" 라벨만 남고 그 밑이 통째로 비었다. 새 다이브의 기본 상태라 흔하다.
> 이제 마지막은 **날짜**다. `date: init?.date ?? todayISO()`라 어떤 경로로도 비지 않는다.
>
> `date` 히어로는 **폴백 전용**이라 주인공 후보 줄(§10-㉜)에는 안 나온다 — 넣으면 모든
> 카드에 날짜 칩이 하나씩 생기는데 그건 다른 이야기(주인공 종류 추가)다.
> 크기는 재지 않고 46 고정이다. 표기가 항상 `YYYY.MM.DD` 10자라 잴 이유가 없다.
>
> **교훈: "필수 항목"과 "빈 문자열이 아님"은 다른 말이다.** 필수는 UI가 요구한다는 뜻이지
> 값이 채워져 있다는 보장이 아니다 — §2-①이 문턱을 낮게 두라고 하므로 앱은 빈 채로
> 저장되는 걸 막지 않는다. 필수 항목을 폴백의 근거로 삼을 때는 `trim()`을 확인해라.

**④ 눈금 축은 0~40m 기본, 넘으면 20 단위로 확장.**
major 20m / minor 10m는 축이 늘어나도 고정한다 — 비례 분할하면 0/30/60 같은 어색한
라벨이 나온다. `maxDepth`가 없으면 축과 눈금은 그리되 액센트 점만 안 그린다.

**⑤ 물기둥이 어두워지는 기준은 절대 수심 40m다.**
축 최대값 기준으로 하면 축이 60으로 늘어났다고 45m 다이브가 40m보다 밝아진다.
수심을 안 적었으면 중립값(0.45)을 쓴다.
*시안의 `.column`은 고정 그라데이션이라 이 규칙이 구현돼 있지 않았다 — 스펙을 따랐다.*

**⑥ 시안의 눈금 좌표는 베끼면 안 된다 — 서로 안 맞는다.**
마크 위치를 역산하면 약 11.5px/m인데 24.5m 카드의 점은 그 축에서 약 22.5m 자리에 있다.
손으로 찍은 값이다. 눈금 좌표(0m=26, 40m=486)만 기준으로 쓰고 마크는 `depthToY()`로 계산한다.

**⑦ 코스틱은 애니메이션을 뺐고, 범위는 CSS를 실측해서 맞췄다.**
결과물이 캡처된 정지 PNG라 애니메이션은 출력에 나타나지 않는다. 그리고 시안의
`inset:-20%; height:62%`를 풀면 카드 안에서 보이는 건 **위 42%뿐**이고 상단에서 이미
마스크가 0.68이다. "62%"를 그대로 쓰면 카드 전체를 덮는다 (한 번 그렇게 만들었다가 고쳤다).
`<Mask>`는 웹에서 안 먹어서, 선 자체를 세로 페이드 그라데이션으로 칠하는 방식으로 바꿨다.

**⑧ 시간 표기는 종목별로 나눈다.** 프리 `2:08` (초 단위 저장), 스쿠버 `42분` (분 단위 저장).
구분자 `:`와 단위 `분`은 시안처럼 작게 깐다.

**⑨ 웹은 카드 눈으로 보는 용도 전용이다.**
`app.json`의 `web.output`은 `single`(SPA). `static`은 SSG를 시도하다 네이티브 모듈에서
터진다. `expo-media-library` / `react-native-view-shot`은 웹 구현이 없어
`captureCard.web.ts` 스텁으로 갈라 뒀다 — **그래서 저장 경로는 웹에서 검증할 수 없다.**

**⑩ 폰트는 weight마다 별도 패밀리명으로 등록한다.**
RN(특히 Android)은 `fontFamily`+`fontWeight` 조합을 제대로 해석하지 않는다.
`src/design/type.ts`가 완성된 스타일만 내보내고 화면에서 `fontFamily`를 직접 쓰지 않는다.
CSS 단위 변환(`em` letter-spacing, 무단위 line-height)도 여기서 끝낸다.
IBM Plex Mono는 **깊은 경로로 import** 한다 — 패키지 루트에서 가져오면 안 쓰는 14개 weight가
전부 번들된다.

**⑪ 생물명 크기는 재지 않고 글자 폭에서 계산한다.**
`adjustsFontSizeToFit`은 Android에서 불안정하고, 1080 캡처 시 화면과 다른 배율로 줄어
결과물이 갈린다. **측정하지 않는 것**이 핵심이다 — 화면이든 1080이든 같은 값이 나와야 한다.
글자 폭 합(한글·CJK ≈1em, 라틴 ≈0.55em)으로 콘텐츠 폭 284를 나눠 **26~56px**로 자르고,
남는 건 2줄 제한이 받는다 (`speciesSize()`).

원래 `≤6자 38 / ≤10자 30 / 그 외 24` 세 단 티어였다. 38은 수심형 히어로(76)의 정확히
절반이라 **카드 3종이 한 가족으로 안 보였다** — 생물형만 유독 조용했다. 이제 76 : 56이다.
`site` 히어로(장소명)도 같은 함수를 쓴다.

**⑫ 한글이 섞이면 mono 대신 Pretendard로 받는다.**
IBM Plex Mono에는 한글 글리프가 없어 그냥 두면 시스템 폰트로 떨어지고, 라틴용 자간이
한글에 붙어 성기게 벌어진다. 지역명·학명 줄처럼 사용자가 한글을 넣을 수 있는 자리는
`hasHangul()`로 갈라 준다.

**㉘ 눈금자는 시안보다 굵다 — 소비되는 크기가 기준이다.**
시안 값(축 1px·투명도 .45→.06, 눈금 1px, 마크 r=3)은 카드를 360px로 볼 때의 값이다.
스토리를 피드에서 보는 폭은 **~300px**이고, 거기서는 전부 1px 미만·10% 미만이 되어
그냥 사라진다. **서명이 소비되는 크기에서 안 보이면 서명이 아니다.** 줄이지 마라.
눈금자를 손대면 카드 폭을 300px로 좁혀서 확인한다 (§7 검증).

그리고 **점 대신 면적으로 읽히게 했다.** 수면(0m)부터 마크까지 축을 액센트로 채운다 —
점은 축소에 가장 약한 형태지만 면적은 남는다. 마크·채움·markline은 자리가 셋인 게 아니라
**하나의 수심 지시자**이므로 §5의 "카드당 한 곳"은 그대로다. 한 자리의 면적이 커진 것.

**㉙ 마크에서 히어로까지 리드선을 잇는다 — 눈금자를 장식에서 정보로.**
콘텐츠가 상단 7%(meta)와 하단 27%(hero+strip)에만 있어서 가운데 58%가 비어 있었다.
게다가 24.5m 마크는 카드 중앙(y≈308)에, "24.5m" 숫자는 하단(y≈500)에 있어서 **읽는 사람이
둘을 연결할 방법이 없었다.** 눈금자가 정보가 아니라 장식이던 이유다. 축을 따라 리드선을
내리고 히어로 글자 쪽으로 꺾어 둘을 잇는다.

액센트 카드(수심형·횟수형)에서만 그린다 — 생물형에서는 눈금자가 생물을 가리키는 셈이라
뜻이 맞지 않고, 이름이 커져서(⑪) 가운데가 이미 차 있다.

히어로는 `marginTop:'auto'`로 앉아서 y를 미리 계산할 수 없다. `DiveCard`가 `onLayout`으로
재서 넘긴다 — 부모가 카드 전체를 덮는 `absoluteFill`이라 `layout.y`가 곧 카드 좌표이고,
`s.u`로 나눠 360 좌표계로 되돌린다. 레이아웃 → 상태 → 재렌더가 한 번 더 도는데,
**저장 경로는 화면 밖 카드를 캡처하기 전에 이미 두 프레임을 기다리므로**
(`app/trip/[id]/cards.tsx`) 캡처 시점에는 반영돼 있다.

**㉛ SVG 그라데이션 `id`는 카드마다 갈라야 한다 — 웹에서 문서 전역이다.**
react-native-svg는 웹에서 실제 DOM SVG를 그리는데, `id`는 **문서 전역**이라 카드를 여러 장
렌더하면 `url(#col)`이 문서의 **첫 번째** `#col`을 잡는다. 즉 모든 카드가 1번 카드의
그라데이션을 쓴다. 실제로 두 가지가 조용히 깨져 있었다:
- 생물형 카드의 수심선이 1번 카드의 **코럴**로 칠해져 액센트가 두 곳이 됐다 (§5 위반).
- **§10-⑤("깊이 들어간 날일수록 어둡다")가 통째로 죽었다** — 18m 카드와 45m 카드가 같아진다.
  카드 목록 화면이 정확히 이 조건(여러 장 동시 렌더)이라 항상 걸린다.

`svgIds(useId(), …)`로 인스턴스마다 접미사를 붙인다 (`src/design/scale.ts`).
`useId()`가 주는 `:r1:`은 `url(#…)`에서 위험하므로 영숫자만 남긴다.
**네이티브에서는 드러나지 않는다** — 새 `<Defs>`를 넣을 때 웹에서 카드를 두 장 이상
띄워 놓고 확인해라.

**㉚ 대문자+넓은 자간 mono 라벨은 kicker 하나만 갖는다.**
region(.14em) · kicker(.2em) · cellKey(.16em) 셋이 전부 그 질감이라 같은 소리를 세 번
내고 있었다. region의 대문자를 뺐다. 위계도 76 하나에 나머지가 전부 9–15px이라 평평했다 —
`place` 17 / `cellValue` 17 / `date` 12.5로 **중간 단**을 만들었다.
결과 위계: `76 / 26–56 / 17 / 12.5 / 9.5–11`.

---

**㉜ 주인공을 사용자가 바꿀 수 있다 — 저장된 선택은 명령이 아니라 힌트다.**
`Dive.heroOverride`. 카드 화면에서 히어로 글자를 탭하면 후보가 열리고, 고르면 즉시 반영된다.

- **선택 UI는 카드 안에 절대 못 들어간다.** 카드 컴포넌트는 1080 캡처에도 같은 것이 렌더되므로
  (§10-①) 카드 내부에 두면 결과물 PNG에 찍힌다. 탭 대상은 카드를 덮는 **형제 오버레이**이고
  후보 트레이는 카드 밖 화면 영역이다. `DiveCard`의 props는 이 기능으로 바뀌지 않았다.
- **무효한 선택은 지우지 않고 읽을 때 무시한다.** 수심을 주인공으로 골라 놓고 수심 값을 지우면
  조용히 자동 규칙으로 돌아가고(경고 없음), 다시 넣으면 선택이 되살아난다. 라벨만 남고 숫자가
  빈 카드는 어떤 경로로도 나올 수 없다 (§2-②).
- **자동 규칙이 고를 것을 고르면 저장하지 않는다.** "자동" 항목을 따로 두지 않는 이유는
  자동-수심과 수동-수심이 카드에서 완전히 똑같아 보이기 때문이다 — 구별 안 되는 두 상태를
  보여주면 뭘 골랐는지 영원히 모른다. 되돌리기는 "원래 것을 다시 고르기"와 같은 동작이 된다.
- **값이 있는 후보만 존재한다.** 비활성·회색·"안 적음" 칩은 §2-② 위반이다 (화면으로 나간 회색
  대시). 후보가 1개뿐인 다이브(필수 3개만 적은 경우)는 탭 대상 자체가 안 생긴다 — 눌리는데
  반응 없는 상태를 만들지 않는다.
- **하트는 주인공을 덮지 않는다.** 수동 선택이 최종이다. 그래서 편집기 문구를 고쳤다 —
  "하트를 누르면 카드 주인공이 됩니다"는 이제 지킬 수 없는 약속이다. 하트가 실제로 하는 일
  (그날의 하이라이트 지정, §2-④)만 말한다.
- 순환(탭할 때마다 다음 것)은 기각했다. 후보 수가 다이브마다 1~4개로 다르고, 되돌리려면 한 바퀴를
  더 돌아야 하며, 지금 뭘 고를 수 있는지 안 보인다.

**미검증:** 선택 직후 저장했을 때의 1080 PNG. 웹은 `Platform.OS==='web'`에서 즉시 return이라
화면 밖 렌더 트리가 마운트조차 안 된다. **override로 처음 생기는 조합**(원래 횟수형이던 카드가
생물형 2줄이 되는 등)은 §10-㉖(lineHeight 클리핑)이 웹에서 재현되지 않으므로 실기기에서 한 번 봐야 한다.

---

**㉝ 트립 기간은 다이브 날짜를 밀어내지 못한다.**
기간 칩과 시작일이 `startDate`/`endDate`만 바꾸고 다이브 날짜를 그대로 뒀더니, 범위 밖 다이브는
**날짜 칩이 아예 없어서** 자기 날짜를 볼 수도 되돌릴 수도 없었다 (값은 저장소에 살아 있는데
화면에 없다). 다른 칩을 누르는 순간 덮어써진다.
- **범위 밖에 남는 다이브가 하나라도 있으면 기간 변경을 거절한다.** 데이터를 안 건드리는 쪽이다.
  검사 하나로 시작일을 뒤로 미는 경우(앞쪽 다이브가 걸림)까지 덮인다.
- **왜 안 되는지 반드시 알린다** — 날짜와 개수를 찍고 다음 행동을 준다. 눌렀는데 반응이 없으면
  고장으로 보인다. 제목이 "기간을 줄일 수 없습니다"가 아닌 이유: 이미 미아가 있는 데이터에서는
  *늘리다가* 막히는 경우도 있어서 그 문장이 거짓이 된다.
- **`+ 다이브 추가`가 물려받는 날짜도 가둬야 한다** (§10-⑭). 안 그러면 미아 날짜를 물려받아
  **새 미아를 만든다.** 자를 때 `startDate`/`endDate`가 아니라 **`daysOfTrip()` 결과**에서 골라라
  — 60일 컷이 있어서 1년짜리 트립이면 기간 안이지만 칩 밖인 날짜가 존재한다.
- **복구 기능은 만들지 마라.** 이미 미아가 된 데이터는 기간 UI로 못 빠져나오고 날짜를 덮어쓰는
  수밖에 없다 — 그래서 "복구 흐름이 필요하다"고 생각하기 쉬운데, **미아를 만드는 경로가 위 두
  개뿐이고 둘 다 막혔다.** 실제 저장소도 확인했고 미아는 없었다 (테스트 브라우저 안에서만
  만들어졌다 지워졌다). 없는 상태를 위한 UI를 만드는 건 §12(트립 요약 카드)와 같은 낭비다.
  새 경로가 생기면 그때 이 줄을 다시 봐라.

**㉞ 저장 실패를 삼키지 않는다 — 재시도하고, 안 되면 화면에 띄운다.**
`loadDB`는 "저장된 값이 깨졌다고 앱이 못 뜨면 안 된다"며 방어하는데 **`saveDB`는 무방비였다.**
`void saveDB(db)`라 거부되면 unhandled rejection이고, 저녁 내내 쓴 8다이브가 안 저장된 걸
사용자가 모른다. 앱 전체가 로컬 저장 하나에 얹혀 있어서(§6) 여기가 제일 아프다.
- 간격 `[200, 600, 1500]`로 재시도하고, 실패하면 `notify('저장되지 않았습니다', …)`.
- **⚠️ 옛 스냅샷을 재시도하면 안 된다.** 실패한 db를 붙들고 재시도하면 그 사이 적은 것이 날아간다.
  버전 번호 + `latest.current`로 **매 시도마다 최신 db를 다시 집는다.**
- 연속 실패 중 모달은 한 번만(`warned`). 한 번이라도 성공하면 풀린다.
- 이것 때문에 `ConfirmProvider`가 `TripProvider` **바깥**이어야 한다 (`_layout.tsx`).

**㉟ "더 적기" 뒤에 숨는 항목을 추가하면 `hasOptional()`도 같이 고쳐라.**
시각(`time`)이 빠져 있었다. 시각 입력칸은 "더 적기" 안에 있는데 `hasOptional()`이 그걸 안 봐서,
**시각만 적은 다이브는 다시 열었을 때 자기 값이 어디에도 안 보였다** — 볼 수도 지울 수도 없다.
접기 판정과 접히는 내용은 **같은 목록을 봐야 한다.**

---

**㊱ 사진 파일명에 타임스탬프를 붙인다 — 같은 이름으로 덮어쓰면 옛 사진이 보인다.**
`${diveId}.${ext}`로 고정이었는데, 사진을 바꿔도 **uri 문자열이 똑같아서** React는 prop이 안
바뀐 줄 알고 RN `Image`는 uri로 캐시한 옛 사진을 그대로 보여줬다. 사용자는 새 사진을 골랐는데
화면은 안 바뀐다. jpg→png처럼 확장자가 달라지면 옛 파일도 영영 남았다.
→ `${diveId}.${base36시각}.${ext}`. 새로 쓰기 전에 그 다이브의 옛 파일을 **확장자와 무관하게**
전부 치운다. **웹에서는 재현되지 않는다** — 사진 피커가 네이티브 전용이다 (§10-⑨).

**㊲ `captureRef`가 만든 파일은 캐시에 남는다.** 사진첩으로 복사한 뒤 지우지 않으면 저장 한 번에
1080×1920 PNG가 하나씩 쌓인다. 카드 저장은 §7의 핵심 동작이라 반복 호출된다.
권한이 없어 못 넣은 경우에도 지운다.

**㊳ 부수효과를 `setDb` 업데이터 안에 넣지 마라.** 사진 파일 삭제가 업데이터 안에 있었다 —
업데이터는 순수해야 하고 **React 19 StrictMode는 개발 중 두 번 부른다.** 삭제 대상은
`latest.current`(§10-㉞이 이미 들고 있다)에서 찾아 업데이터 **밖에서** 지운다.

**㊴ 기본값을 `??`로 채운 객체에서 `...init`은 맨 위여야 한다.**
```ts
{ date: init?.date ?? todayISO(), ...init }   // ✗ init.date가 명시적 undefined면 도로 지워진다
{ ...init, date: init?.date ?? todayISO() }   // ✓
```
`??`는 spread 전에 이미 계산이 끝나 있어서, 뒤에 오는 spread가 그 결과를 덮는다.

**㊵ 선택돼도 굵기를 바꾸지 않는다 — 색만 바꾼다. (`Chip` · `Segmented` 둘 다)**
`fontWeight: '600'`이 붙어 있었는데, 폰트가 weight마다 별도 패밀리라 **네이티브에서는 무시되고
react-native-web에서만 굵어졌다** (§10-⑩). 웹에서 본 것과 실기기가 다르다. 게다가 굵어지면 글자
폭이 변해 고를 때마다 줄바꿈이 재계산돼 칩이 손가락 밑에서 움직인다.
⚠️ 처음엔 `Chip`만 고치고 **`Segmented`(종목 선택)를 놓쳤다.** 새로 넣지 마라 — `fontWeight`가
`src/` 어디에도 없는 게 맞는 상태다 (`grep -rn fontWeight src/`).

**㊶ 열린 스와이프 행은 화면에 하나뿐이다.**
여럿 열려 있으면 "지우기"가 여러 개 보여서 어느 것이 방금 민 것인지 헷갈리고 실수로 다른 행을
지운다. 트립 목록·다이브 목록이 같은 컴포넌트를 쓰므로(§10-㉔) 목록이 상태를 내려주는 대신
`SwipeRow` **모듈 수준**에서 관리한다 — 호출부를 안 건드린다.
⚠️ 신원 비교용 객체를 **렌더 중에 만지면 안 된다.** `useRef` 안의 객체를 쓰되 `.current`는
콜백·이펙트에서만 읽어라 (`react-hooks/refs`, 그리고 useState 값 직접 수정도 같은 이유로 막혀 있다).

**㊷ `loadDB`는 깨진 항목을 버리지 않고 고친다.**
`trips`가 배열인지만 봤더니, 트립에 `dives`가 없으면 화면의 `[...trip.dives]`에서 터졌다.
그렇다고 트립을 통째로 버리면 사용자 기록이 날아간다 — 화면이 실제로 건드리는 필드
(`title.trim()` · `region.split()` · `[...dives]`)만 채워 준다. **id 없는 것만 버린다**
(고쳐도 가리킬 대상이 없다).

---

**㊸ 시뮬레이터 UI 자동화는 `idb`로 한다 — 접근성 권한이 필요 없다.**
"자동화 불가"라고 적어 뒀던 건 틀렸다. 실패 원인은 시뮬레이터가 아니라 **터미널에 접근성 권한이
없어서**였다 (`System Events` → `-1719`). orca·AppleScript·CGEvent가 전부 여기서 막혔다.
`idb`는 CoreSimulator에 직접 말을 걸어서 권한 없이 동작한다.
```bash
brew tap facebook/fb && brew trust facebook/fb && brew install idb-companion
python3 -m venv <venv> && <venv>/bin/pip install fb-idb
<venv>/bin/idb ui tap   --api hid --udid <UDID> <x> <y>          # 좌표는 **포인트**(픽셀÷3)
<venv>/bin/idb ui swipe --udid <UDID> --duration 0.45 --delta 12 x1 y1 x2 y2
```
- **스와이프에 `--duration`·`--delta`가 없으면 스크롤이 안 먹는다.** 순간이동으로 처리된다.
- 화면 확인은 `xcrun simctl io <UDID> screenshot`. 딥링크(`divelog://trip/<id>`)로 화면을
  건너뛰면 탭 수가 확 준다.
- **Metro 인스펙터(CDP)로 앱 안에서 JS를 돌리는 건 실패했다** — 붙자마자 프록시가 끊는다(1006).
  네이티브 검증은 idb 쪽으로 가라.

**㊹ 9:16 크롭은 우리가 한다 — 피커에게 맡길 수 없다.**
`expo-image-picker`의 `allowsEditing` + `aspect: [9,16]`을 썼는데 **`aspect`는 Android 전용이라
iOS에서 조용히 무시된다.** 시뮬레이터 실측:
```
가로 1600×900 → 1200×675  (원본 비율 그대로)
세로  900×1600 →  679×679  (정사각형으로 잘림)
```
정사각으로 잘린 사진이 카드의 `resizeMode="cover"`에서 **또** 잘리므로 손실이 두 번이고,
사용자가 고른 크롭이 카드에서 어떻게 남을지 예측할 수 없었다.
→ 크롭 UI를 끄고(원본을 받는다) `expo-image-manipulator`로 **가운데를 9:16으로 한 번만** 자른다.
카드의 `cover`와 같은 규칙이라 **피커에서 본 것이 카드에 그대로 나온다.** 탭도 하나 줄었다.
긴 변은 1080×1920(EXPORT 크기)으로 맞춘다 — 카드가 그보다 크게 쓸 일이 없다.

**㊺ 사진 경로를 절대경로로 저장하지 마라 — 앱 컨테이너 UUID는 바뀐다.**
`file:///…/Application/<UUID>/Documents/dive-photos/x.jpg`를 그대로 저장했더니, 앱을 다시
설치하자 **파일은 멀쩡한데 경로만 죽어서 사진이 통째로 사라졌다** (썸네일이 빈칸). iOS는 앱을
업데이트할 때 이 UUID를 바꾼다. 사진을 캐시에서 문서 디렉토리로 옮기는 이유가 "몇 년 뒤에도
살아 있게"인데 절대경로가 그 목적을 무너뜨리고 있었다.
→ `resolvePhotoUri()`가 **파일명만 믿고 경로를 매번 다시 만든다.** 불러올 때(`storage.ts`의
`repairDive`) 한 번 통과시키므로 이미 깨진 데이터도 그 자리에서 복구된다.

**㊻ 피커·매니퓰레이터가 캐시에 남긴 사본을 지운다.** `persistPhoto`가 복사만 하고 원본을
안 지워서 캐시에 그대로 쌓였다. 테스트 이미지는 KB 단위라 안 보였지만 실제 사진은 장당 몇 MB다.
복사가 끝나면 `discardCache()`로 치운다 (문서 디렉토리 안의 것은 건드리지 않는다).

---

**㊼ 테스트는 화면 없이 도는 부분만 본다 — `npm test` (vitest, 64개).**
타입체크와 lint는 이 앱에서 고친 버그를 **하나도** 못 잡았다. 전부 화면을 눌러서 찾았다.
그래서 테스트의 역할은 발견이 아니라 **이미 고친 것이 다시 깨지지 않게 잡아 두는 것**이다.
단위는 "데이터가 저장소를 한 바퀴 돌아 카드가 될 때까지" —
`addDive` → `saveDB` → `loadDB` → `repair` → `resolveHero` → `cellsFor`.

- **§2-②를 불변식으로 적는다.** 빈 값 조합을 전부 만들어 놓고 **부재로** 단언한다 —
  "Depth 칸이 있다"가 아니라 **"값 없는 칸이 하나도 없다"**. 기본 픽스처는 전 항목 채운
  다이브가 아니라 **필수 3개만 적힌 것**과 **`site: ''`인 방금 추가한 다이브**다.
- **묶음 1·2는 `src/app`·`src/components`·`src/ui`를 import하지 않는다.** 16개 파일에
  `throw`를 심고 48개가 통과하는 것으로 확인했다 — 화면을 고쳐도 안 깨진다.
  (㉒를 검사하는 `confirm.test.tsx`만 예외다. `src/ui/Confirm`을 실제로 렌더한다.)
- **카드 렌더 결과는 테스트하지 않는다.** §5는 카드가 계속 조정돼 왔다고 말한다. 스냅샷으로
  굳히면 조정마다 깨지고, **깨진 스냅샷을 기계적으로 갱신하는 습관이 §10-㉛ 같은 조용한
  회귀를 오히려 승인한다.** 눈금자를 손대면 300px로 눈으로 본다(§10-㉘) — 그건 사람 일이다.
- **러너 TZ는 `America/Los_Angeles` 고정.** `fromISO`를 UTC 파싱으로 되돌려 보면 LA에서
  7건이 잡히고 서울에서는 1건만 잡힌다. 한국에서만 돌리면 날짜가 하루 밀리는 회귀를 놓친다.
- 잡지 못하는 것: ㉝·㉟(규칙이 화면 파일 안에 있다 — 테스트 때문에 옮기지 않기로 했다),
  ㊶(제스처), 네이티브 전용 전부(㉕㉖㉗㊱㊲㊹㊻).

**㊽ 테스트 대역(fake)은 통과시키는 가짜가 아니라 차단기여야 한다.**
`expo-file-system` 대역의 `copy`·`delete`가 조용한 no-op이었다. 그러면 `persistPhoto`가
자르기도 복사도 **아무것도 안 하고 그럴듯한 문서 경로를 돌려주면서 테스트가 초록이 된다.**
모킹으로 네이티브 모듈 버그(㊱㊲㊹㊻)를 못 잡는 이유가 이것이다 — 가짜는 정의상 문서대로
행동한다. 지금은 파일을 만지는 모든 호출이 **던진다.** 파일 I/O를 검사하고 싶어지면
대역을 고치지 말고 시뮬레이터로 가라 (§10-㊸).

**㊾ `npm run lint`는 `expo lint`가 아니라 `eslint .`다.**
`expo lint`는 자기가 정한 대상만 보고 **`test/`를 건너뛴다** — 테스트에 에러를 심고 대조해서
확인했다. 그대로 두면 테스트를 고쳐도 lint가 조용해서 **검증 장치 쪽이 먼저 썩는다.**
규칙은 `eslint-config-expo` 그대로이고 바뀌는 건 "무엇을 보는가"뿐이다.
대신 생성물·작업 산출물은 `ignores`로 뺀다 (`dist` · `.expo` · `.claude` · `ios` · `android`).

**⚠️ `act()` 경고는 vitest 기본 리포터에 가려진다.** `npx vitest run --reporter=verbose`로만
보인다. 리액트 상태를 건드리는 호출은 `act` 안에서 해라 — 지금은 뒤따르는 `await act(...)`가
흘려 주더라도, 그 경고는 "단언이 낡은 트리를 볼 수 있다"는 신호이고 버전이 오르면 순서 의존이 된다.

---

### 입력 화면에서 정한 것

**⑬ 저장하는 값과 계산하는 값을 나눈다 — 트립 제목, 누적 번호.**
둘 다 저장하지 않고 **볼 때마다 만든다.**
- 제목: §4의 "비면 자동 생성"은 빈 문자열이 곧 "자동"이라는 뜻이다. 생성 시점에 구우면
  지역·기간이 아직 비어 있어서 `"8.5"` 같은 껍데기가 남고, 나중에 채워도 안 따라온다.
- 누적 번호: 저장하면 빠뜨린 다이브를 중간에 끼워 넣을 때 뒤 번호가 전부 틀어진다.
  실제로 몰아 쓰다가 "그날 한 번 더 탔지" 하고 끼워 넣는다. 날짜순으로 매번 세면 항상 맞는다.

**⑭ 다이브 날짜는 달력이 아니라 트립 일자 칩으로 고른다.**
`1일차 8.5(수) / 2일차 8.6(목) / …`. 트립이 기간을 이미 알고 있으니 달력을 띄울 이유가 없다.
8개를 몰아 쓸 때 탭 수가 확 줄어든다. 새 다이브는 **직전 다이브의 날짜·종목을 물려받는다.**

**⑮ 프리다이빙 시간 입력 — §9 해소.**
프리는 `분`/`초` 두 칸, 스쿠버는 `분` 한 칸. 초에 60 이상을 적으면 분으로 올려 준다
("130초"라고 적는 사람이 있다). 종목을 바꾸면 실제 길이를 유지하도록 환산하고,
종목 전용 항목(공기압 / CWT)은 지운다.

**⑯ 하트는 다이브당 하나만.** 카드 주인공이 둘일 수 없다 (§3). 다른 걸 누르면 옮겨간다.

**⑰ 새 트립은 폼 없이 바로 만든다.** 만들고 트립 화면에 들어가서 제목·지역을 고친다.
시작하기 전에 넘을 문턱을 두지 않는 게 §2-①과 같은 태도다.

**⑱ 빈 칸을 잘못처럼 보이게 하지 않는다.**
필수 3개조차 별표를 안 붙인다. 그냥 위에 있고, 나머지는 "더 적기" 뒤에 접혀 있다.

**⑲ react-native-web 주의 — `minWidth: 0`.**
RN(yoga)은 flex 자식의 min-width가 0이지만 웹은 CSS `min-width:auto`를 따라간다.
가로로 나란한 입력칸에서 단위(`m`, `°C`)가 칸 밖으로 밀려 잘렸다. 공용 필드에 넣어 뒀다.

**㉑ 날짜 표기와 목록 구조.**
`지역 · 8.5–8.7 · 2박 · 다이브 3`처럼 가운뎃점으로 늘어놓으면 날짜가 묻힌다.
- 표기는 **`08.05 수`** — 자리수를 채워 mono로 찍는다. 폭이 항상 같아서 목록에서 열이
  딱 맞고, 카드가 쓰는 `2026.08.02`와 같은 결이다. 요일이 있어야 "수요일에 갔지"가 산다.
- 목록은 **연도로 묶는다.** 헤더에 `2026 … 트립 2 · 다이브 5`. 연도 헤더가 연도를
  책임지므로 행에는 월·일만 남고, "지난 해면 연도를 붙인다" 같은 예외가 필요 없어진다.
  누적이 자산이라는 §3의 전제와도 결이 맞다 — 해마다 얼마나 탔는지가 보인다.
- 목록 행은 **시작일만** 오른쪽에 세운다. 기간은 제목이 이미 말한다("제주 2박").
- 상세는 장소 줄과 숫자 줄을 나눈다. `2박`은 날짜 범위와 같은 말이라 뺐다.
- **자동 제목에 날짜를 넣지 않는다.** 목록·상세 모두 제목 바로 옆에 날짜가 있어서
  `Bali 11.2` 같은 중복이 생겼다. 지금은 `제주 2박` / 당일이면 `제주` / 지역이 없으면 `새 트립`.

### 실기기(시뮬레이터)에서만 드러난 것

웹에서 아무리 봐도 안 보이던 버그가 둘 있었다. 카드 저장은 **반드시 네이티브에서** 확인해야 한다.

**㉕ `captureRef`는 포인트 크기를 기기 배율로 곱한다.**
카드를 1080**포인트**로 렌더하고 캡처했더니 @3x 기기에서 **3240×5760(16MB)** 이 나왔다.
@2x 기기였다면 2160×3840 — **기기마다 결과물이 달라진다.** "어디서나 같은 1080×1920"이
이 구조의 존재 이유였는데 정반대였다.
→ `exportRenderWidth()` = `EXPORT_W / PixelRatio.get()` 포인트로 렌더한다.
   (포인트 × 배율 = 정확히 1080px, 글자는 여전히 기기 배율로 래스터화되어 선명하다)

**㉗ `expo-media-library`의 `saveToLibraryAsync`는 SDK 57에서 호출하면 throw한다.**
"Method saveToLibraryAsync ... is deprecated" 예외가 **런타임에** 난다. 타입체크도
빌드도 통과하므로 실기기에서 저장 버튼을 눌러 보기 전까지 드러나지 않는다.
→ 새 클래스 API `Asset.create(uri)`를 쓴다. (`expo-image-picker`는 같은 함정 없음)

**㉖ RN의 `Text`는 `lineHeight`가 1em보다 작으면 글자를 자른다.**
시안의 `line-height:.86`을 그대로 옮겼더니 캡처에서 "24.5"의 2와 5 윗부분이 잘렸다.
CSS는 넘친 글자를 그냥 보여 주지만 RN은 라인 박스에 클리핑한다.
**react-native-web은 CSS 동작을 따르므로 웹에서는 재현되지 않는다.**
→ 큰 숫자(`figure`)는 leading을 주지 않는다. 1em 미만 leading은 어디에도 쓰지 마라.

**㉔ 밀어서 지우기는 직접 만들었다. 남의 라이브러리로는 웹에서 안 됐다.**
왼쪽으로 밀면 "지우기"가 나온다 (`src/ui/SwipeRow.tsx`). **트립 목록과 다이브 목록에
같은 컴포넌트를 쓴다.** 다만 펼쳐진 다이브 편집기는 감싸지 않는다 — 입력 중에 밀리면
곤란하고, 거기엔 이미 "지우기" 버튼이 있다. 여기까지 오는 데 함정이 셋이었고
전부 웹에서만 드러났다.
- `react-native-gesture-handler`의 `Swipeable`은 **웹에서 pan이 활성화되지 않는다.**
  포인터 이벤트는 다 들어오는데(down/move/up 계측 확인) 행이 1px도 안 움직인다.
  RN 코어의 `PanResponder` + `Animated`로 직접 만들면 웹·네이티브가 같은 코드로 돈다.
- **`onMoveShouldSetPanResponderCapture`여야 한다.** 안쪽 Pressable이 터치 시작에
  이미 응답자를 가져가서, 비-capture 핸들러는 호출조차 되지 않는다.
- **드래그 직후의 탭을 막아야 한다.** 응답자를 뺏어와도 브라우저는 mouseup 뒤에 DOM
  click을 따로 쏘고, RNW Pressable이 그걸 받아 onPress를 또 부른다 — 밀 때마다 상세
  화면으로 넘어가 버렸다. 그래서 탭을 `SwipeRow`가 직접 받고 드래그 후 320ms는 무시한다.
- `useNativeDriver`는 웹에서 지원되지 않는다 (`Platform.OS !== 'web'`로 가른다).

**㉒ `Alert.alert`을 쓰지 않는다. 웹에서 아무 일도 일어나지 않는다.**
react-native-web에서는 다이얼로그가 뜨지도, 콜백이 돌지도 않는다 — "지우기"를 눌러도
조용히 무시된다. 확인창이 통째로 죽는데 에러도 안 난다. 게다가 네이티브 Alert은 OS
기본 스타일이라 이 앱의 어두운 계기판 톤과 이질적이다.
**`useConfirm()`을 쓴다** (`src/ui/Confirm.tsx`) — Modal 하나로 처리하고 Promise로 답을 준다.
```ts
const ok = await confirm({ title: '지울까요?', destructive: true });
await notify('저장했습니다', '1080×1920');
```

**⚠️ 이건 큐다. 하나만 띄우는 구조가 아니다.**
처음엔 resolver를 하나만 들고 있었는데, 두 번째 요청이 **이전 resolve를 부르지 않고 덮어썼다** —
`await confirm(...)`이 영영 안 끝나고(Promise 누수), 사용자는 답한 적 없는데 삭제 확인창이
사라졌다. 스토어가 아무 때나 저장 실패를 알릴 수 있게 되면서(§10-㉞) 실제로 도달 가능해졌다.
- **대기 중인 Promise는 반드시 결말이 나야 한다.** 요청마다 자기 resolve를 들고 줄을 선다.
- **떠 있는 모달은 사용자가 답할 때만 닫힌다.** 파괴적 확인이 알림에 밀려나는 그림 자체를 없앤다.
  ("밀려나는 걸 false로 resolve"는 누수만 막고 확인창이 사라지는 건 그대로라 안 골랐다.)
- **모달 사이에 420ms를 둔다** (`HANDOFF_MS`). 곧바로 갈아 끼우면 방금 "확인"을 누른 손가락이
  같은 자리의 다음 버튼을 그대로 누른다 — 다음이 삭제 확인이면 못 보고 지운다.
  실측: 닫은 순간부터 그 좌표가 죽는 데 11ms, 되살아나는 데 437ms. 300ms 오탭은 안 닿는다.
  **줄이면 안 된다.** 반대로 ~437ms보다 느린 더블탭은 닿으므로 420이 상한이다.

**㉓ 이름을 문장에 끼우지 않는다 — 조사가 틀어진다.**
`"세부 리브어보드"을 지울까요?`가 나왔다. 받침 유무로 을/를이 갈리는데 영문 이름까지
오면 규칙이 감당이 안 된다. **제목 문장은 조사 없이 고정하고 이름은 본문에 따로 둔다.**

**⑳ `<Link asChild>`를 쓰지 않는다. 자식의 style을 통째로 덮어쓴다.**
목록 행을 `<Link asChild><Pressable style={...}>`로 감쌌더니 패딩도 `flexDirection`도
사라져서, 오른쪽에 있어야 할 하트와 `›`가 제목 아래로 떨어졌다. DOM을 보니 `<a>`의
computed style이 `padding:0; flex-direction:column` — style이 전달되지 않았다.
**이동은 `router.push()`로 한다.** 눌러서 넘어가는 곳은 전부 그렇게 통일했다.

---

## 11. iOS 빌드가 갑자기 깨졌다면 — 경로의 공백

**이 프로젝트는 공백이 든 경로에 있다** (`/Volumes/P31 2TB/…` — 볼륨 이름에 공백).
Expo·React Native의 iOS 빌드 스크립트 몇 개가 경로를 따옴표로 감싸지 않아서 깨진다.
세 곳을 패치해 뒀는데 **전부 재생성되는 디렉토리 안에 있어서 사라진다.**

**사라지는 조건**
- `npm install`, `npx expo install <pkg>` → ①② 소멸
- `expo prebuild --clean` → ③ 소멸
- 다른 머신에서 클론 → 전부 없음

**증상 두 가지 — 두 번째가 고약하다**
1. 빌드 실패: `bash: /Volumes/P31: No such file or directory`
2. **빌드는 성공하는데** 앱 실행 시 빨간 화면:
   `expo-linking needs access to the expo-constants manifest`
   → ②가 조용히 `exit 0` 해서 `app.config`가 생성되지 않은 것이다. 빌드 로그에는
   아무 흔적이 없으니 이 증상을 보면 바로 ②를 의심해라.

**복구 — 세 곳 다 "따옴표 씌우기"가 전부다**

① `node_modules/expo-constants/ios/EXConstants.podspec` (40행 근처)
   `$PODS_TARGET_SRCROOT/…` 를 작은따옴표로 감싼다. `bash -c "문자열"`은 그 문자열을
   명령줄로 파싱하므로 공백에서 쪼개진다.
```ruby
:script => "bash -l -c \"#{env_vars}'$PODS_TARGET_SRCROOT/../scripts/get-app-config-ios.sh'\"",
```

② `node_modules/expo-constants/scripts/get-app-config-ios.sh` (14행)
   bash에서 인용 없는 `basename`은 `P31`을 돌려주고, `!= "Pods"` 조건에 걸려 조기 종료한다.
```bash
PROJECT_DIR_BASENAME=$(basename "$PROJECT_DIR")
```

③ `ios/app.xcodeproj/project.pbxproj` — "Bundle React Native code and images" 단계.
   백틱 실행을 인용된 명령 치환으로 바꾼다.
```
"$("$NODE_BINARY" --print "require('path').dirname(require.resolve('react-native/package.json')) + '/scripts/react-native-xcode.sh'")"
```

①을 고친 뒤에는 `cd ios && pod install`을 다시 돌려야 Pods 프로젝트에 반영된다.

**근본 해결 (아직 안 함)**
- 볼륨 이름에서 공백 제거 (`diskutil rename "P31 2TB" "P31-2TB"`) — 가장 깔끔하다.
  같은 디스크의 다른 프로젝트도 함께 해결된다. 스캔해 보니 셸 설정·LaunchAgent·심볼릭
  링크에 이 경로를 박아 둔 곳은 없었고, 영향받는 건 `ios/Pods`의 절대경로 243개
  (→ `pod install`로 해결)와 에디터 최근 항목 정도다.
- 또는 `patch-package`로 ①②를 고정 (③은 별도).

---

## 12. 트립 요약 카드 — 만들었다가 뺐다

§2-③이 "개별 다이브 카드보다 더 잘 올라간다"고 해서 설계·구현까지 갔는데,
**실물을 보고 불필요하다고 판단해 제거했다.** 코드도 지웠다
(`TripCard` · `DepthProfile` · `tripStats` · `tripFields`).

**왜 적어 두는가** — §2-③의 문장만 남겨 두면 다음에 이어 작업할 때 똑같은 걸 또 만든다.

**어떻게 만들었었나** (다시 시도한다면 여기서 출발하지 마라)
- 주인공: 다이브 수 ("8 다이브") — 항상 존재하는 유일한 값이라 골랐다
- 그래픽: 수심 눈금자의 확장. 다이브마다 점 하나, 세로=수심, 가로=시간순, 일자 구분선
- 집계 칸: MAX / TOTAL / TEMP / SITES
- 배경: 하이라이트 지정 다이브의 사진 → 없으면 물기둥

**만들면서 드러난 것들** (개념을 되살릴 때 다시 밟게 될 함정)
- 개별 카드와 같은 수심축(0m=y26, 40m=y486)을 쓰면 **27m보다 깊은 점이 히어로 숫자와 겹친다.**
  개별 카드는 눈금자가 왼쪽 얇은 기둥이라 안 겹쳤을 뿐이다.
- 가장 깊은 점에서 축까지 리드선을 그으면 **히어로 숫자를 관통해 취소선처럼 보인다.**
- 수심을 하나도 안 적은 트립에서는 **눈금만 남은 빈 차트**가 된다 (§2-② 위반).
- 다이브가 1개면 요약이 그 다이브 카드와 같은 말을 한다.

**결론:** 집계 숫자를 모은 카드는 올리고 싶은 그림이 아니었다. 카드의 단위는 다이브다.
